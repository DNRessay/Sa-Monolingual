{
  "Comment": "sa-languages-corpus: fetch every (language, source) pair, resuming each in chunks until its stream is exhausted or a safety cap is hit, then process each language into a clean deduped file.",
  "StartAt": "FetchAll",
  "States": {
    "FetchAll": {
      "Type": "Map",
      "ItemsPath": "$.pairs",
      "ResultPath": "$.fetchResults",
      "MaxConcurrency": 6,
      "ItemProcessor": {
        "ProcessorConfig": { "Mode": "INLINE" },
        "StartAt": "InitFetchLoop",
        "States": {
          "InitFetchLoop": {
            "Type": "Pass",
            "Parameters": {
              "lang.$": "$.lang",
              "source.$": "$.source",
              "skip": 0,
              "chunk": 200000,
              "iteration": 0
            },
            "Next": "FetchChunk"
          },
          "FetchChunk": {
            "Type": "Task",
            "Resource": "${FetchFunctionArn}",
            "ResultPath": "$.result",
            "Retry": [
              {
                "ErrorEquals": ["States.ALL"],
                "IntervalSeconds": 15,
                "MaxAttempts": 3,
                "BackoffRate": 2.0
              }
            ],
            "Next": "AdvanceFetchLoop"
          },
          "AdvanceFetchLoop": {
            "Type": "Pass",
            "Parameters": {
              "lang.$": "$.lang",
              "source.$": "$.source",
              "skip.$": "$.result.next_skip",
              "chunk.$": "$.chunk",
              "iteration.$": "States.MathAdd($.iteration, 1)",
              "exhausted.$": "$.result.exhausted"
            },
            "Next": "FetchLoopDone"
          },
          "FetchLoopDone": {
            "Type": "Choice",
            "Choices": [
              { "Variable": "$.exhausted", "BooleanEquals": true, "Next": "FetchSucceed" },
              { "Variable": "$.iteration", "NumericGreaterThanEquals": 25, "Next": "FetchSucceed" }
            ],
            "Default": "FetchChunk"
          },
          "FetchSucceed": { "Type": "Succeed" }
        }
      },
      "Next": "ProcessAll"
    },
    "ProcessAll": {
      "Type": "Map",
      "ItemsPath": "$.langs",
      "ResultPath": "$.processResults",
      "MaxConcurrency": 3,
      "ItemSelector": {
        "lang.$": "$$.Map.Item.Value"
      },
      "ItemProcessor": {
        "ProcessorConfig": { "Mode": "INLINE" },
        "StartAt": "ProcessLang",
        "States": {
          "ProcessLang": {
            "Type": "Task",
            "Resource": "${ProcessFunctionArn}",
            "Retry": [
              {
                "ErrorEquals": ["States.ALL"],
                "IntervalSeconds": 30,
                "MaxAttempts": 2,
                "BackoffRate": 2.0
              }
            ],
            "End": true
          }
        }
      },
      "Next": "PushAll"
    },
    "PushAll": {
      "Type": "Map",
      "ItemsPath": "$.langs",
      "ResultPath": "$.pushResults",
      "MaxConcurrency": 3,
      "ItemSelector": {
        "lang.$": "$$.Map.Item.Value"
      },
      "ItemProcessor": {
        "ProcessorConfig": { "Mode": "INLINE" },
        "StartAt": "PushLang",
        "States": {
          "PushLang": {
            "Type": "Task",
            "Resource": "${PushFunctionArn}",
            "Retry": [
              {
                "ErrorEquals": ["States.ALL"],
                "IntervalSeconds": 30,
                "MaxAttempts": 2,
                "BackoffRate": 2.0
              }
            ],
            "End": true
          }
        }
      },
      "End": true
    }
  }
}
